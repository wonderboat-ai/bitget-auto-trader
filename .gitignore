# Segredos - NUNCA versionar
.env
*.key
*.secret

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Logs e estado local
logs/*.log
logs/*.jsonl
state/*.json
# Sobra da escrita atômica (.tmp -> replace): em pasta sincronizada o replace
# pode falhar e deixar o .tmp para trás. Nunca é código; não versionar.
state/*.tmp

# Dados de mercado e cache do dossiê (regeneráveis via research/download_data*.py
# e dossier_fetch.py — não são código, não precisam ir pro repositório)
/data/

# Pesquisa: datasets e resultados grandes (regeneráveis); mantém os scripts .py e os
# relatórios .md, que são o que importa versionar
/research/data/
/research/data_2b/
/research/data_perp/
/research/data_3/
/research/results/
/research/results_2b/
/research/results_3/
/research/scratch/
/research/scratch3/
research/*.jsonl

# Dossiê diário gerado (arquivo histórico, não é código)
/Dossie Cripto/

# Trilha de auditoria arquivada na transição testnet->mainnet (27/07/2026) —
# mesmo espírito de logs/*.jsonl acima: histórico, não código
/Historico-Testnet-*/

# Configuração local desta máquina (caminhos absolutos, específicos deste PC)
.claude/
.mcp.json

# Repositório git ANINHADO não relacionado (dashboard separado, tem seu próprio .git)
/repo/

# OS
.DS_Store
Thumbs.db
